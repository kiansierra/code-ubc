base_save_folder:  '../input/UBC-OCEAN-PROCESSED'
name: crops-MT-${max_tiles}_SS-${split_size}_RF-${resize_factor}
image_folder: '../input/UBC-OCEAN/train_images'
save_folder: ${base_save_folder}/${name}
dataframe_path: ../input/UBC-OCEAN/train-components.parquet
resize_factor: 0.5
split_size: 512
min_resize:  4000
extension: 'png'
empty_threshold:  0.5
max_tiles: Null
center_crop: True
backend: pyvips
num_processes: 4